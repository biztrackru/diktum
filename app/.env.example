# Copy this file to .env and fill local secrets.
# Never commit .env.

# Hugging Face token with read access to pyannote/speaker-diarization-community-1.
# Used only to download the diarization model on first run. Audio never leaves
# this machine. Keep this file private (setup stores it with chmod 600).
HF_TOKEN=hf_your_token_here
